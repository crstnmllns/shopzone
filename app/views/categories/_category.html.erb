<tr scope="row">
  <td><%= category.name %></td>
  <td><%= link_to 'Update', edit_category_path(category), class: 'btn-sm btn-success text-white text-decoration-none'%>
      <%= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?', remote: true }, class: "delete btn-sm btn-danger text-white text-decoration-none" %>
</tr>
